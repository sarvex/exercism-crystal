{%- assign first = true -%}
require "spec"
require "../src/*"

describe "{{- json.exercise_cammel -}}" do
{%- for cases in  json.cases -%}
    {% if cases.cases %}
        {%- for subcases in cases.cases %}
        {% include "./bin/extra-templates/generator_help", first: first %} "{{ subcases.description }}" do
        {{ json.exercise_cammel }}.{{ subcases.property | underscore }}("{{ subcases.input.color }}").should eq({{ subcases.expected }})
    end
        {% endfor %}
    {% else %}  
    {% include "./bin/extra-templates/generator_help", first: first %} "{{ cases.description }}" do
        {{ json.exercise_cammel }}.{{ cases.property | underscore }}().should eq({{ cases.expected }})
    end
    {% endif %}
{% endfor %}
end
