require "spec"
require "../src/*"

describe "<%-= to_capitalized(@json["exercise"].to_s) %>" do
<%- @json["cases"].as_a.each do |cases| %>
    <%= status()%> "<%-= cases["description"] %>" do
        sentence = <%= cases["input"]["sentence"].inspect %>
        expected = <%= cases["expected"].as_h %>
        <%= to_capitalized(@json["exercise"].to_s) %>.<%= to_snake(cases["property"].to_s)%>(sentence).should eq(expected)
    end
<% end %>
end
